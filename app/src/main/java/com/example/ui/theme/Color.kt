package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Minimalist Monochromatic Light Palette
val LightBackground = Color(0xFFF4F5F6) // Soft, uniform off-white
val LightSurface = Color(0xFFFFFFFF)    // Pure white for borderless cards
val LightSurfaceVariant = Color(0xFFEAEAEA) // Slightly darker gray for subtle details/inputs
val LightBorder = Color(0x00000000)     // Borderless!

// Minimalist Monochromatic Dark Palette
val DarkBackground = Color(0xFF0F1011)  // Rich, modern deep black
val DarkSurface = Color(0xFF18191B)     // Dark charcoal for cards
val DarkSurfaceVariant = Color(0xFF242528) // Lighter charcoal for inputs/details
val DarkBorder = Color(0x00000000)      // Borderless!

// Single Premium Brand Accent (Monochrome Deep Slate/Charcoal)
val AccentColorLight = Color(0xFF111111) // Pure dark charcoal/black for highlights
val AccentColorDark = Color(0xFFEEEEEE)  // Off-white for dark mode highlights

val GrayMedium = Color(0xFF7F8487)
val GrayLight = Color(0xFFD2D5D6)

// Functional colors with minimal subtle hints (if necessary, but keeping them very soft)
val SoftGreen = Color(0xFF2E7D32)
val SoftRed = Color(0xFFC62828)

val TextWhite = Color(0xFFF9FAFB)
val TextGray = Color(0xFF8E9294)
val TextDark = Color(0xFF111111)
val TextMuted = Color(0xFF6B7280)
